[pytest]
testpaths = tests
markers =
    serial: timing-sensitive test that's flaky under full-suite CPU load — collected last (see tests/conftest.py) and best run in its own process (see scripts/run_tests.py).
